#include <stdio.h>

int main(void) {
    for (double w = 0.9, u = 0.1, b = -51, up = -0.98, bp = 153, x, h = 0, t = 0; (!t && scanf("%lf", &x) || 1) && t < 5; ++t && printf("%.1f ", x = up * (h = w * x + u * h + b) + bp));
    return 0;
}